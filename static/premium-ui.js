function toggleSidebar(){document.getElementById('sidebar')?.classList.toggle('mobile-open')}
function copyResult(id='ai-result'){const el=document.getElementById(id); if(!el)return; navigator.clipboard.writeText(el.innerText||el.value||'')}
function downloadResult(id='ai-result',name='careermate-result.txt'){const el=document.getElementById(id); if(!el)return; const blob=new Blob([el.innerText||el.value||''],{type:'text/plain'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=name; a.click(); URL.revokeObjectURL(a.href)}
async function sendChat(e){e.preventDefault(); const input=document.getElementById('user-input'); const box=document.getElementById('chat-box'); const text=input.value.trim(); if(!text)return; box.insertAdjacentHTML('beforeend',`<div class="flex justify-end"><div class="chat-user max-w-[82%] p-4">${escapeHtml(text)}</div></div>`); input.value=''; box.scrollTop=box.scrollHeight; const typing=document.createElement('div'); typing.className='flex justify-start'; typing.innerHTML='<div class="chat-ai max-w-[82%] p-4 text-muted">CareerMate is thinking...</div>'; box.appendChild(typing); box.scrollTop=box.scrollHeight; try{const res=await fetch('/get',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:'msg='+encodeURIComponent(text)}); const data=await res.text(); typing.innerHTML=`<div class="chat-ai max-w-[82%] p-4">${formatBot(data)}</div>`}catch(err){typing.innerHTML='<div class="chat-ai max-w-[82%] p-4 text-danger">AI connection error. Check Flask terminal and Gemini quota.</div>'} box.scrollTop=box.scrollHeight}
function escapeHtml(s){return s.replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]))}
function formatBot(s){try{const j=JSON.parse(s); s=j.response||j.answer||j.message||s}catch(e){} return escapeHtml(String(s)).replace(/\n/g,'<br>')}
